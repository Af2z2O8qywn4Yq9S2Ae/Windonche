{
  "name": "windonche",
  "version": "2.1.0",
  "private": true,
  "type": "module",
  "description": "Thèmes Windows 95/98 pour Onche — userscript modulaire",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run build && npm test && node --check dist/windonche.user.js",
    "test:browser": "node tests/browser.mjs"
  },
  "engines": { "node": ">=22" },
  "license": "MIT"
}
